{
  "name": "surveybuilder",
  "private": true,
  "description": "",
  "main": "index.js",
  "version": "1.0.1",
  "license": "ISC",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "bootstrap": "npx lerna bootstrap",
    "publish": "npx lerna publish",
    "build": "lerna bootstrap & lerna run build --scope @surveybuilder/observable & lerna run build --scope @surveybuilder/fhir-converter & lerna run build --scope @surveybuilder/models & lerna run build --scope @surveybuilder/player & lerna run build --scope @surveybuilder/designer",
    "dev": "yarn workspace demo-app install",
    "start": "cd demo-app && yarn start"
  },
  "dependencies": {
    "lerna": "^3.13.0"
  },
  "workspaces": [
    "demo-app",
    "packages/*"
  ]
}